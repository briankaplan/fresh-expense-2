import { ExpenseCategory, ExpenseCategoryType } from "@fresh-expense/types";

// User related types
// Auth related types
// Expense related types
// Re-export the types from @fresh-expense/types for convenience
export { ExpenseCategory, ExpenseCategoryType };

// API related types
// Common component props
// Route related types
